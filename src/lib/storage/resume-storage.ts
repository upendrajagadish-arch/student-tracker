/** @deprecated Import from `@/lib/storage` instead. Re-exported for compatibility. */
export {
  buildResumeFileName,
  getStorageProvider,
  sanitizeFileName,
  setStorageProvider,
  type StorageProvider,
} from "./index";
